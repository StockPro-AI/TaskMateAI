version: '3.8'

services:
  taskmateai:
    image: nginx:alpine
    container_name: taskmateai-web
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped
    networks:
      - taskmateai-network

networks:
  taskmateai-network:
    driver: bridge
